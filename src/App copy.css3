* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Inter, Arial, sans-serif;
  background: #f5f8fc;
  color: #14213d;
}

button,
input {
  font: inherit;
}

button {
  cursor: pointer;
}

.app {
  min-height: 100vh;
}

.navbar {
  height: 76px;
  padding: 0 6%;
  background: rgba(255, 255, 255, 0.96);
  border-bottom: 1px solid #e7edf5;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: sticky;
  top: 0;
  z-index: 10;
  backdrop-filter: blur(12px);
}

.logo {
  display: flex;
  align-items: center;
  gap: 11px;
}

.logo-icon {
  width: 42px;
  height: 42px;
  border-radius: 13px;
  display: grid;
  place-items: center;
  background: #e8f4ff;
  font-size: 22px;
}

.logo strong {
  display: block;
  font-size: 21px;
}

.logo small {
  display: block;
  color: #8a98aa;
  font-size: 10px;
  margin-top: 2px;
}

.nav-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.search {
  height: 40px;
  display: flex;
  align-items: center;
  border: 1px solid #dfe7f0;
  background: white;
  border-radius: 22px;
  overflow: hidden;
}

.search input {
  border: 0;
  outline: 0;
  width: 150px;
  padding: 0 13px;
}

.search button {
  border: 0;
  background: transparent;
  padding: 0 12px;
}

.location-button {
  border: 1px solid #dfe7f0;
  background: white;
  border-radius: 22px;
  padding: 9px 14px;
}

.avatar {
  width: 39px;
  height: 39px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: #14213d;
  color: white;
  font-size: 12px;
  font-weight: bold;
}

main {
  width: min(1200px, 94%);
  margin: auto;
  padding: 30px 0 50px;
}

.message {
  background: #fff4d6;
  color: #725600;
  padding: 11px 15px;
  border-radius: 12px;
  margin-bottom: 15px;
  font-size: 13px;
}

.hero {
  min-height: 350px;
  padding: 48px;
  border-radius: 30px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  overflow: hidden;
  background:
    radial-gradient(circle at 80% 30%, rgba(255, 216, 87, 0.45), transparent 28%),
    linear-gradient(135deg, #dff2ff, #eef8ff 60%, #fff9e8);
}

.eyebrow {
  color: #1876d1;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.8px;
  margin: 0 0 12px;
}

.hero h1 {
  font-size: clamp(38px, 5vw, 58px);
  line-height: 0.98;
  margin: 0;
  letter-spacing: -2px;
}

.hero h1 span {
  color: #1674d1;
}

.hero-description {
  max-width: 520px;
  color: #65768d;
  line-height: 1.6;
  margin: 18px 0 24px;
}

.profile-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.profile-tabs button {
  border: 1px solid #d7e2ee;
  background: white;
  padding: 10px 15px;
  border-radius: 22px;
  transition: 0.2s;
}

.profile-tabs button:hover,
.profile-tabs .selected {
  background: #14213d;
  color: white;
  transform: translateY(-2px);
}

.weather-orb {
  width: 235px;
  height: 235px;
  flex-shrink: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.78);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-shadow: 0 25px 60px rgba(39, 82, 120, 0.15);
}

.big-weather-icon {
  font-size: 43px;
}

.weather-orb strong {
  font-size: 58px;
  line-height: 1;
  margin: 5px 0;
}

.weather-orb p {
  margin: 0;
  font-weight: 700;
}

.weather-orb small {
  margin-top: 5px;
  color: #8492a5;
}

.loader {
  color: #65768d;
  font-size: 13px;
}

.location-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 15px 3px 35px;
}

.location-row div {
  display: flex;
  align-items: center;
  gap: 8px;
}

.location-row div span:last-child {
  color: #8997a8;
  font-size: 12px;
}

.live-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #21a56b;
  box-shadow: 0 0 0 4px #e1f7ec;
}

.location-row button {
  border: 0;
  background: transparent;
  color: #1876d1;
  font-weight: 700;
}

.section-heading {
  display: flex;
  justify-content: space-between;
  align-items: end;
  margin-bottom: 14px;
}

.section-heading p,
.panel-heading p,
.alert-header p {
  color: #1876d1;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.5px;
  margin: 0 0 5px;
}

.section-heading h2,
.panel-heading h2,
.alert-header h2 {
  margin: 0;
  font-size: 22px;
}

.section-heading > span {
  color: #8b98aa;
  font-size: 11px;
}

.smart-card {
  background: #14213d;
  color: white;
  border-radius: 21px;
  padding: 23px;
  display: flex;
  align-items: center;
  gap: 18px;
  box-shadow: 0 15px 35px rgba(20, 33, 61, 0.12);
}

.smart-icon {
  width: 54px;
  height: 54px;
  flex-shrink: 0;
  border-radius: 15px;
  display: grid;
  place-items: center;
  font-size: 25px;
  background: rgba(255, 255, 255, 0.11);
}

.smart-text {
  flex: 1;
}

.smart-text h3 {
  margin: 0 0 7px;
  font-size: 15px;
}

.smart-text p {
  margin: 0;
  color: #c4cfdd;
  font-size: 13px;
  line-height: 1.5;
}

.best-time {
  border-left: 1px solid rgba(255, 255, 255, 0.17);
  padding-left: 25px;
  min-width: 100px;
}

.best-time small {
  display: block;
  color: #9eacc0;
  font-size: 9px;
  margin-bottom: 5px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 13px;
  margin: 16px 0;
}

.stat-card {
  background: white;
  border: 1px solid #e7edf4;
  border-radius: 17px;
  padding: 18px;
}

.stat-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.stat-card small {
  color: #8996a8;
  font-size: 9px;
  font-weight: 800;
}

.stat-top span {
  color: #24a36b;
  font-size: 8px;
  font-weight: 800;
}

.stat-card strong {
  display: block;
  font-size: 28px;
  margin: 12px 0 5px;
}

.stat-card p {
  margin: 0;
  color: #8795a8;
  font-size: 11px;
}

.content-grid {
  display: grid;
  grid-template-columns: 1.65fr 1fr;
  gap: 15px;
  margin-top: 15px;
}

.panel {
  background: white;
  border: 1px solid #e7edf4;
  border-radius: 20px;
  padding: 21px;
}

.panel-heading {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.panel-heading > span {
  color: #1876d1;
  font-size: 10px;
}

.days {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 5px;
  margin-top: 20px;
}

.day {
  text-align: center;
  padding: 11px 4px;
  border-radius: 13px;
}

.day:first-child {
  background: #edf7ff;
}

.day small {
  color: #8390a2;
  font-size: 10px;
}

.day > span {
  display: block;
  font-size: 22px;
  margin: 10px 0;
}

.day strong {
  display: block;
  font-size: 14px;
}

.day em {
  display: block;
  color: #9ba6b5;
  font-size: 10px;
  margin-top: 3px;
  font-style: normal;
}

.day label {
  display: block;
  color: #4394d6;
  font-size: 8px;
  margin-top: 9px;
}

.sun-times {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-top: 25px;
}

.sun-times div {
  background: #f7faff;
  padding: 16px;
  border-radius: 15px;
}

.sun-times span {
  display: block;
  font-size: 24px;
  margin-bottom: 8px;
}

.sun-times small {
  display: block;
  color: #8a98aa;
  font-size: 10px;
}

.sun-times strong {
  display: block;
  font-size: 18px;
  margin-top: 3px;
}

.daylight-tip {
  margin-top: 12px;
  padding: 12px;
  border-radius: 12px;
  background: #fff6df;
  color: #765d16;
  font-size: 10px;
}

.second-grid {
  align-items: stretch;
}

.hours {
  display: flex;
  gap: 7px;
  overflow-x: auto;
  margin-top: 20px;
  padding-bottom: 4px;
}

.hour {
  min-width: 68px;
  text-align: center;
  background: #f7faff;
  border-radius: 12px;
  padding: 11px 5px;
}

.hour small {
  color: #8a97a8;
  font-size: 9px;
}

.hour span {
  display: block;
  margin: 8px 0;
}

.hour strong {
  display: block;
  font-size: 15px;
}

.hour label {
  display: block;
  color: #4c93cc;
  font-size: 8px;
  margin-top: 6px;
}

.destination-item {
  width: 100%;
  border: 0;
  border-bottom: 1px solid #edf1f5;
  background: white;
  padding: 14px 3px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  text-align: left;
}

.destination-item:last-child {
  border-bottom: 0;
}

.destination-item strong {
  display: block;
  font-size: 14px;
}

.destination-item small {
  display: block;
  color: #8997a8;
  margin-top: 4px;
  font-size: 9px;
}

.destination-item > span {
  color: #1876d1;
  font-size: 18px;
}

.empty {
  color: #8a97a8;
  font-size: 12px;
}

.alert-center {
  margin-top: 15px;
  padding: 22px;
  background: white;
  border: 1px solid #e7edf4;
  border-radius: 20px;
}

.alert-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.alert-status {
  color: #269b67;
  font-size: 9px;
  font-weight: 800;
}

.alert-item {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 15px;
  padding: 13px;
  background: #fff8e8;
  border-radius: 12px;
}

.alert-item span {
  font-size: 18px;
}

.alert-item p {
  margin: 0;
  font-size: 12px;
  color: #6d5a27;
}

.how-section {
  margin-top: 45px;
  padding: 30px;
  border-radius: 25px;
  background: #eaf5ff;
}

.how-section h2 {
  margin: 0;
  font-size: 28px;
}

.logic-cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  margin-top: 22px;
}

.logic-cards div {
  background: white;
  border-radius: 16px;
  padding: 20px;
}

.logic-cards span {
  display: block;
  color: #1876d1;
  font-size: 11px;
  font-weight: 800;
  margin-bottom: 15px;
}

.logic-cards strong {
  display: block;
}

.logic-cards p {
  color: #7f8da0;
  font-size: 11px;
  line-height: 1.5;
}

footer {
  padding: 28px 6%;
  border-top: 1px solid #e5ebf2;
  background: white;
  display: flex;
  justify-content: space-between;
  color: #8996a7;
  font-size: 11px;
}

footer strong {
  color: #14213d;
}

@media (max-width: 850px) {
  .nav-actions .search {
    display: none;
  }

  .hero {
    padding: 32px;
  }

  .weather-orb {
    width: 190px;
    height: 190px;
  }

  .weather-orb strong {
    font-size: 45px;
  }

  .content-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 650px) {
  .navbar {
    padding: 0 15px;
  }

  .location-button {
    display: none;
  }

  main {
    width: 94%;
    padding-top: 15px;
  }

  .hero {
    flex-direction: column;
    align-items: flex-start;
    gap: 25px;
    padding: 27px;
  }

  .hero h1 {
    font-size: 38px;
  }

  .weather-orb {
    align-self: center;
  }

  .location-row {
    align-items: flex-start;
    gap: 12px;
    flex-direction: column;
  }

  .stats-grid {
    grid-template-columns: 1fr 1fr;
  }

  .smart-card {
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .best-time {
    width: 100%;
    border-left: 0;
    border-top: 1px solid rgba(255,255,255,.17);
    padding: 13px 0 0;
  }

  .days {
    overflow-x: auto;
    grid-template-columns: repeat(7, 72px);
  }

  .logic-cards {
    grid-template-columns: 1fr;
  }

  .section-heading {
    align-items: flex-start;
    gap: 10px;
    flex-direction: column;
  }

  footer {
    flex-direction: column;
    gap: 9px;
  }
}

@media (max-width: 420px) {
  .stats-grid {
    grid-template-columns: 1fr;
  }

  .hero h1 {
    font-size: 34px;
  }
}